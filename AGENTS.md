# neovim-config Repo Rules

## Auto Commit & Push
When work on this repo is completed (code fix, feature, or any change):
1. `git add -A`
2. `git commit -m "type: description"`
3. `git push`
Do this immediately after finishing without asking.
Skip only if there are no changes to commit.
